[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shellraiser"
version = "1.0.0"
description = "A bash to C transpiler"
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
run-my-script = "shellraiser:main"
